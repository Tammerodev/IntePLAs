{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++ build active file",
            "command": "/bin/bash", // Use bash to execute a shell command
            "args": [
                "-c", // -c option tells bash to read commands from the command string 
                      // TODO / FIX / TO DO / DO NOW: OPTIMIZATION ARE DISABLED FOR DEBUGGING!!!
                "g++ ${workspaceFolder}/src/*.cpp -pg -g -Wall -Wno-unused-function -Wno-unused-variable -lsfml-system -lsfml-window -lsfml-graphics -lsfml-audio -lsfml-network -ltgui -ljsoncpp -o ${workspaceFolder}/bin/${fileBasenameNoExtension}" // Shell command to compile all .cpp files in src folder
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}
