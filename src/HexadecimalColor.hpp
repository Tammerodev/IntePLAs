#pragma once
#include <SFML/Graphics/Color.hpp>
#include <string>

class HexadecimalColor {
    public:
        void readHexString(const std::string hex_string) {

        }

        const sf::Color getHexColor() {
            
        }
    private:
        long long hexColor = 0x000000;
};