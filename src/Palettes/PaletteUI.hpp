#pragma once
#include <SFML/Graphics/Color.hpp>

namespace Palette {
    namespace PaletteUI {
        const sf::Color White        = sf::Color(246, 246, 246);
        const sf::Color DarkerWhite  = sf::Color(232, 232, 232);
        const sf::Color Gray         = sf::Color(51, 51, 51);
        const sf::Color WineRed      = sf::Color(153, 1, 0);
        const sf::Color Red          = sf::Color(185, 5, 4);    
        const sf::Color Yellow       = sf::Color(255, 214, 10);    
        
    }
}